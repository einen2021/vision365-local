/**
 * Firebase config stub — db and storage are mock objects.
 * Real data lives in data/db.json (see /api/db).
 */

export const db = { __jsonDb: true };
export const storage = { __jsonStorage: true };
